apiVersion: v1
kind: Service
metadata:
  name: {{ include "kics.fullname" . }}
  labels:
    {{- include "kics.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "kics.selectorLabels" . | nindent 4 }}
